const form = document.getElementById('form-validacao')

function validaNumero(validacao) {
    document.getElementById('#n1', '#n2')
    if( '#n1' < '#n2') return true
    if( '#n1' > '#n2') return false
}

form.addEventListener('submit', function(e) {
    e.preventDefault();
})

console.log(form);